/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      // ── INSERT your image hosting domain here ──
      // e.g. "res.cloudinary.com", "your-s3-bucket.s3.amazonaws.com"
    ],
  },
};

module.exports = nextConfig;
